
@using Core.Transcoder.Utils.Resources

@if (Request.Cookies["User"] != null)
{
   

    //ViewBag.Title = UiStrings.home_auth_title;

    @Html.Partial("_HomeAuth")
    
}
else
{
    @Html.Partial("_HomeVisiteur")
}
